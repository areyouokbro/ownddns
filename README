# 🚀 Cloudflare DDNS Manager

一个基于 Cloudflare API 的自动 DDNS 工具，支持一键安装、自动更新 DNS、定时运行与一键卸载。

适用于：

- 家庭宽带动态 IP
- VPS / 服务器 IP 变化
- 自建节点 / 域名解析自动更新
- IPv4 / IPv6 自动解析

---

# 📦 1. 一键安装（推荐）

```bash
bash <(curl -sL https://raw.githubusercontent.com/你的用户名/你的仓库/main/install.sh) \
  -k "CF_API_KEY" \
  -u "你的邮箱" \
  -z "example.com" \
  -h "sub.example.com" \
  -t A
